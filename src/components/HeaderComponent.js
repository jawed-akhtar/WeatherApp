import React from 'react';

const Header = (props) => {
    return (
        <div className="container">
            <header>
                <h1 className="py-4" >Weather App</h1>
            </header>
        </div>
    );
}

export default Header;